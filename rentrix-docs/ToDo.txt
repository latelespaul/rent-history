- For local dev convenience, you may want new reviews to be APPROVED by default. Change status: 'PENDING' → 'APPROVED' if you'd rather see them immediately. I'll default to PENDING so admin page has something to do.

// sharing db ->
// approved db ->
// aadhar based verification
// change git repo names
// impl theme in ui
// maker and checker schema
// checker schema
// chnage

// read about aws setup
// use digi locker for user verification

// validate user and it's input (is pin, society valid)
// Create rental docs api and feature so that use can upload it and write reviews