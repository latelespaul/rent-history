# API
VITE_API_BASE_URL=/api/v1
VITE_APP_NAME=Rentio

# Mock mode — set to "true" to use in-file mocks (no backend needed)
# Set to "false" once Spring Boot security + endpoints are ready
VITE_ENABLE_MOCKS=true